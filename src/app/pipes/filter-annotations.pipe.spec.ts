import { FilterAnnotationsPipe } from './filter-annotations.pipe';

describe('FilterAnnotationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAnnotationsPipe();
    expect(pipe).toBeTruthy();
  });
});
