import {Component} from '@angular/core';

import {BroadcastComponent} from './broadcast.component';

@Component({
  selector: 'app-broadcasting-users',
  templateUrl: './broadcasting-users.component.html',
  styleUrls: ['./broadcasting-users.component.scss'],
})

export class BroadcastingUsersComponent extends BroadcastComponent { }
