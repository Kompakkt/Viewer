import { StringifyVectorPipe } from './stringify-vector.pipe';

describe('StringifyVectorPipe', () => {
  it('create an instance', () => {
    const pipe = new StringifyVectorPipe();
    expect(pipe).toBeTruthy();
  });
});
